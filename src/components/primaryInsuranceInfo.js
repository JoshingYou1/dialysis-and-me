import React from 'react';
import {connect} from 'react-redux';

export function PrimaryInsuranceInfo(props) {
    return (
        <div>
            <h1>Primary Insurance Information</h1>
            <section>
                
            </section>
        </div>
    );
}

export default connect()(PrimaryInsuranceInfo);